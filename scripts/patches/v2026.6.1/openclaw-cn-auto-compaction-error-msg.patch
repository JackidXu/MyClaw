diff --git a/src/auto-reply/reply/agent-runner-execution.ts b/src/auto-reply/reply/agent-runner-execution.ts
index 7d13bf3693..2fd7e34435 100644
--- a/src/auto-reply/reply/agent-runner-execution.ts
+++ b/src/auto-reply/reply/agent-runner-execution.ts
@@ -1023,8 +1023,8 @@ function resolveContextWindowForCompactionHint(params: {
 function buildContextOverflowResetHint(contextWindowTokens: number | undefined): string {
   const reserveFloor = computeContextAwareReserveTokensFloor(contextWindowTokens);
   return (
-    "\n\nTo prevent this, increase your compaction buffer by setting " +
-    `\`agents.defaults.compaction.reserveTokensFloor\` to ${reserveFloor} or higher in your config.`
+    "\n\n如需调整，可在配置中调大压缩缓冲区：设置 `agents.defaults.compaction.reserveTokensFloor` 为 " +
+    `${reserveFloor} 或更高。`
   );
 }

@@ -1215,10 +1215,10 @@ export function buildContextOverflowRecoveryText(params: {
   activeSessionEntry?: SessionEntry;
 }): string {
   const prefix = params.preserveSessionMapping
-    ? "⚠️ Auto-compaction could not recover this turn. I kept this conversation mapped to the current session. Please try again, use /compact, or use /new to start a fresh session."
+    ? "⚠️ 当前对话上下文已满且自动压缩失败。已保留当前会话映射，请尝试发送 /compact 压缩对话或发送 /new 开启新对话。"
     : params.duringCompaction
-      ? "⚠️ Context limit exceeded during compaction. I've reset our conversation to start fresh - please try again."
-      : "⚠️ Context limit exceeded. I've reset our conversation to start fresh - please try again.";
+      ? "⚠️ 压缩过程中超出上下文限制。已为您重置对话以重新开始，请重试。"
+      : "⚠️ 超出上下文限制。已为您重置对话以重新开始，请重试。";
   const primaryContextWindow = resolveContextWindowForCompactionHint({
     cfg: params.cfg,
     primaryProvider: params.primaryProvider,
diff --git a/src/auto-reply/reply/agent-runner.runreplyagent.e2e.test.ts b/src/auto-reply/reply/agent-runner.runreplyagent.e2e.test.ts
index 01ceef546d..74a1e22e18 100644
--- a/src/auto-reply/reply/agent-runner.runreplyagent.e2e.test.ts
+++ b/src/auto-reply/reply/agent-runner.runreplyagent.e2e.test.ts
@@ -2266,7 +2266,7 @@ describe("runReplyAgent typing (heartbeat)", () => {
     if (!payload) {
       throw new Error("expected payload");
     }
-    expect(payload.text).toContain("Auto-compaction could not recover this turn");
+    expect(payload.text).toContain("当前对话上下文已满且自动压缩失败");
     expect(payload.text).toContain("reserveTokensFloor");
     expect(payload.text).toContain("/new");
   });
@@ -2289,7 +2289,7 @@ describe("runReplyAgent typing (heartbeat)", () => {
     if (!payload) {
       throw new Error("expected payload");
     }
-    expect(payload.text).toContain("Auto-compaction could not recover this turn");
+    expect(payload.text).toContain("当前对话上下文已满且自动压缩失败");
     expect(payload.text).toContain("reserveTokensFloor");
     expect(payload.text).toContain("/new");
   });
